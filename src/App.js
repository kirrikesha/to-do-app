import React from 'react';
import TodosList from "../src/components/TodosList"

const App = () => {
    return (
        <div>
            <main>
                <TodosList/>
            </main>
        </div>
    );
};

export default App;